# Copy this file to .env and fill in real values:
#   cp .env.example .env
# Never commit .env — it is gitignored.

# LLM provider keys (we'll choose which one to use in Task 4)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# PostgreSQL connection string (set up in Task 13)
DATABASE_URL=
