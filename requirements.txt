# Data validation and settings (used across the whole app)
pydantic>=2

# Loads variables from .env into the environment
python-dotenv

# Testing
pytest

# LLM provider SDKs
groq
google-genai

# PostgreSQL database
psycopg[binary]>=3.1     # modern PostgreSQL driver for Python

# API server
fastapi>=0.100
uvicorn[standard]>=0.20    # the server that runs FastAPI
